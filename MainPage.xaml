<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeApp.MainPage"
             Title="Time"
             BackgroundColor="#F5F5F5">

    <Grid>
        <CollectionView x:Name="EventsList" Margin="16" MaximumWidthRequest="600" HorizontalOptions="Center">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="White" CornerRadius="15" Padding="0" Margin="0,0,0,15" HasShadow="True" BorderColor="#E0E0E0">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnEventTapped" />
                        </Frame.GestureRecognizers>

                        <Grid ColumnDefinitions="*, Auto" Padding="20">
                            <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                                <Label Text="{Binding Title}" FontSize="20" FontAttributes="Bold" TextColor="#333333" />
                                <Label Text="{Binding Date}" FontSize="14" TextColor="Gray" Margin="0,5,0,0" />
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" HorizontalOptions="End" VerticalOptions="Center">
                                <Label Text="{Binding DaysLeft}" FontSize="28" FontAttributes="Bold" TextColor="#3F51B5" HorizontalOptions="Center" />
                                <Label Text="Days Left" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="+" FontSize="28" FontAttributes="Bold" TextColor="White" BackgroundColor="#FF4081" 
                CornerRadius="30" WidthRequest="60" HeightRequest="60"
                HorizontalOptions="End" VerticalOptions="End" Margin="20,20,20,40"
                Shadow="{Shadow Brush=Black, Offset='0,5', Opacity=0.3}"/>
    </Grid>
</ContentPage>